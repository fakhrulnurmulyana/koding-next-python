[project]
name = "koding-next-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "115722602+fakhrulnurmulyana@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = [
    "customtkinter>=6.0.0",
    "marimo>=0.24.1",
]

[project.scripts]
koding-next-python = "koding_next_python:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
